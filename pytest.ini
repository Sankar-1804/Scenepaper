[pytest]
# Lets `import backend...` resolve in src/tests/ without installing the
# package — matches how .github/workflows/lint-test.yml invokes
# `pytest src/tests/` from the repo root.
pythonpath = src
testpaths = src/tests
